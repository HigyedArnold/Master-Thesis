version: '3.3'

services:

  planr:
    image: planr:1.0
    # image: planr:4.0
    expose:
      - 9000
    deploy:
      resources:
        limits:
          cpus: '2.0'
          # cpus: '8.0'
          memory: 1024M
          # memory: 4096M
        reservations:
          cpus: '2.0'
          # cpus: '8.0'
          memory: 1024M
          # memory: 4096M

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - planr
    ports:
      - 8900:8900
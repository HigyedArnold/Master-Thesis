version: '3.3'

services:

  planr:
    image: planr:5.5
    # image: planr:5.0
    expose:
      - 9000
    deploy:
      resources:
        limits:
          cpus: '1.5'
          # cpus: '7.5'
          memory: 768M
          # memory: 3840M
        reservations:
          cpus: '1.5'
          # cpus: '7.5'
          memory: 768M
          # memory: 3840M

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - planr
    ports:
      - 8900:8900
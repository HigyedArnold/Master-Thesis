# Routes
# This file defines all application routes (Higher priority routes first)

# Planr
GET       /test         com.planr.rest.controller.TestController.get()
POST      /solve        com.planr.rest.controller.SolverController.solve()